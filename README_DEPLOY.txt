가격검색 웹 배포용 - 가장 쉬운 버전

구성 파일:
- app.py
- index.html
- requirements.txt
- render.yaml

목표:
Render에 올려서 전 지점에서 URL로 접속하게 만드는 버전입니다.

가장 쉬운 배포 순서:

1. GitHub 가입 / 로그인

2. 새 저장소 만들기
   예: price-search

3. 이 폴더 안의 파일 4개를 GitHub 저장소에 업로드
   - app.py
   - index.html
   - requirements.txt
   - render.yaml

4. Render 가입 / 로그인
   https://render.com

5. New + 클릭
   → Web Service 선택
   → GitHub 저장소 연결
   → price-search 선택

6. 설정
   Name: price-search
   Environment: Python
   Build Command: pip install -r requirements.txt
   Start Command: gunicorn app:app

7. 환경변수 추가
   Key:
   GOOGLE_SERVICE_ACCOUNT_JSON

   Value:
   gpam.json 파일 내용을 메모장으로 열어서 전체 복사 후 붙여넣기

8. Deploy 클릭

9. 배포 완료 후 URL 확인
   예:
   https://price-search.onrender.com

10. 이 URL을 매니저님들께 공유

주의:
- 서비스 계정 JSON은 절대 카톡이나 게시판에 공유하지 마세요.
- Render 환경변수에만 넣으세요.
- 무료 Render는 오래 안 쓰면 잠들 수 있습니다. 처음 접속이 30초 정도 느릴 수 있습니다.
- 웹페이지에서 [동기화] 버튼을 누르면 최신 가격리스트를 다시 읽습니다.


예외상품 설정 기능:
- 상품 카드 하단 버튼 3개 추가
  1. 제조일: 제조일만 적힌 상품 → 기한확인필요 탭
  2. 관리제외: 유통기한 관리 제외 → 비관리대상
  3. 유통기한: 기존처럼 유통기한 계산
- 예: 새로인 파바빈 검색 → [제조일] 클릭

주의:
- Render 무료 서버는 파일 저장이 영구 보장되지 않을 수 있습니다.
- 장기적으로는 예외상품을 구글시트에 저장하는 방식이 더 안정적입니다.


v2 수정:
- 상품 카드 하단에 예외설정 버튼이 실제로 보이도록 index.html 수정
- 버튼: 제조일 / 관리제외 / 유통기한
- 새로인 파바빈 검색 후 [제조일] 버튼을 누르면 기한확인필요로 분류


v3 수정:
- 예외설정 버튼 클릭 시 아무 변화가 없던 오류 수정
- 원인: 버튼 클릭 시 item.product 변수가 화면 밖에서 참조되어 동작하지 않음
- 수정: 상품명을 버튼 생성 시점에 URL 인코딩해서 전달하도록 변경


v4 추가:
- 휴대폰에서 유통기한 직접 수정 기능 추가
- 상품 카드에서 날짜 선택 후 [저장]
- 저장하면 해당 상품은 입력한 유통기한 기준으로 다시 분류
- 예: 새로인 파바빈 → 유통기한 입력 → 저장 → 유통기한관리로 이동

주의:
- 현재 방식은 Render 서버의 exceptions.json에 저장됩니다.
- 무료 Render는 파일 저장이 영구 보장되지 않을 수 있습니다.
- 장기적으로는 예외상품/수정 유통기한을 구글시트에 저장하는 방식이 더 안정적입니다.


v5 추가/수정:
- 유통기한 수정 저장 기준을 상품명 단독에서 '매장+입고일+박스번호+상품명+시트+행' 기준으로 변경
- 같은 상품이라도 매장/박스/입고일이 다르면 각각 다른 유통기한 저장 가능
- 예: A매장 물티슈 7/23, B매장 물티슈 7/8 별도 저장
- search_data.json 요청 시 10분 이상 지난 데이터는 자동 동기화 시도
- 매장에서는 접속할 때마다 별도로 동기화 버튼을 누르지 않아도 됨
- 동기화 버튼은 그대로 유지되어 누구든 필요할 때 수동 갱신 가능


v6 박스 검수 모드 추가:
- 매장 선택 기능 추가
- 상품 검색 / 박스 검수 모드 분리
- 박스 검수: 입고일 + 박스번호 입력 → 해당 박스 상품만 표시
- 유통기한 저장은 '내 매장 + 원본행' 기준으로 저장
- 같은 상품이라도 매장별로 다른 유통기한 저장 가능
- 예: 마곡 물티슈 7/23, 상계 물티슈 7/8 별도 저장


v7 매장 선택 확정:
- 29개 지점 선택창 유지
- 처음 한 번 선택한 매장은 휴대폰 브라우저에 저장
- 이후 접속 시 자동 적용
- 유통기한 수정/제조일/관리제외는 '선택한 매장 기준'으로만 저장
- 다른 매장의 같은 상품에는 영향을 주지 않음

포함 지점:
영등포, 서원, 양재, 당산, 상계, 수서, 가양, 석촌, 강남, 마곡,
신대방3, 가락, 먹골, 흑석, 노원, 녹번, 왕십리, 부평, 하계,
평택안중, 대구, 수원, 갈산, 답십리, 선유도, 석계, 성남태평,
인천논현, 구로구청


v9 단순 모바일형 수정:
- 박스 검수 모드 제거
- 지점 선택창을 오른쪽 상단 작은 선택창으로 이동
- 상단 고정 영역은 제목/지점/동기화만 남겨 검색 결과를 가리지 않게 수정
- 검색창은 접고 펼치는 방식이 아니라 본문에 자연스럽게 배치
- 하단 버튼: 위로 / 검색 / 동기화


v10 하단 빠른검색 추가:
- 하단 고정 영역에 상품명/박스번호 빠른검색창 추가
- 숫자만 입력하면 박스번호 검색으로 처리
- 글자를 입력하면 상품명 검색으로 처리
- 세부검색 영역 접기/펼치기 버튼 추가
- 날짜 등 상세조건은 펼쳐서 사용하고, 간단 검색은 하단에서 바로 가능


v11 전체 UI 정리:
- 박스 검수 관련 HTML/JS 제거
- 지점 선택은 오른쪽 상단에 작게 유지
- 세부검색은 기본 닫힘
- 하단 고정 빠른검색창 추가
  - 숫자만 입력: 박스번호 검색
  - 문자 입력: 상품명/바코드 검색
- 검색 결과를 가리지 않도록 상단 고정 영역 최소화


v12:
- 하단 검색창 제거
- 상품명/박스번호 검색 모드 선택
- 박스번호 입력 즉시 해당 번호만 표시
- 0.2초 자동검색

v13 오류 수정:
- 지점 선택 옵션이 표시되지 않던 JavaScript 오류 수정
- 상단 오른쪽 동기화 버튼 복구
- 상품명/박스번호 즉시검색 유지


v14 복합 가격 표시 개선:
- 가격 셀 원문을 priceRaw로 그대로 보존
- 단일가격은 기존처럼 7,000원으로 표시
- 1,000~3,000 범위 가격은 범위 그대로 표시
- 매장/권역 재량 가격은 '매장·권역 재량 · 최소~최대원'으로 표시
- 신발/잡화/의류처럼 종류별 가격이 여러 줄인 경우 '종류별 가격 · 상세보기' 표시
- 상품 카드의 [가격 상세 보기]를 누르면 가격표 원문을 줄바꿈 그대로 확인 가능
- '부착된 가격'은 '상품 부착가 확인'으로 표시
- 가격 원문도 상품 검색 대상에 포함


v15 동기화/검색 오류 수정:
- 누락된 search() JavaScript 함수 복구
- 동기화 성공 후 데이터가 '없음'으로 보이던 오류 수정
- 데이터 로딩 실패 시 실제 오류 메시지를 화면에 표시
- 동기화 결과가 0건일 경우 실패 시트 정보를 표시


v16 기능 추가:
- 날짜 세부검색에 전체리스트 탭 추가
- 전체리스트는 150건씩 더보기로 확인
- 유통기한 분류에 2개월(60일), 3개월(90일) 추가
- 현재 검색·날짜·분류·매장 조건을 반영한 취합 엑셀 다운로드 추가
- 엑셀에 취합리스트와 유통기한요약 시트 포함
